def XLX_output(database_cursor, output_directory = "Default to current directory"):
    #write selected sheet out to file as .xlx for excel use.
    #should have page of relevant data and title of how data is filtered
    return None
def graphical_output(database_cursor, output_directory = "Default to current directory"):
    #return a graph .jpg image
    # two modes, bar and map. Map should be used for sentiment analysis from certain locations.
    return None